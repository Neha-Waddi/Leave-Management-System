spring.application.name=leave-management-system

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/lms_db
spring.datasource.username=root
spring.datasource.password=password

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# Logging
logging.level.org.springframework=INFO

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JWT Secret Key (minimum 32 characters)
jwt.secret=HarekrishnaHareKrishnaHareRamaHareRamaSecretKey

# 24 hours
jwt.expiration=86400000

#mail config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nehawaddi@gmail.com
spring.mail.password=scfx yloy jfay jesj

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#actuator
management.endpoints.web.exposure.include=*

management.endpoint.health.show-details=always

management.info.env.enabled=true
info.app.name=Leave Management System
info.app.version=1.0
info.app.developer=Neha